; Template configuration file.
;
; Copy this file to .config.ini and fill in real values before use:
;
;   cp config.ini .config.ini
;
; .config.ini is gitignored and should never be committed.

[redshift]
host     = <redshift-cluster-endpoint>
port     = 5439
database = <database-name>
user     = <username>
password = <password>
